<!-- appointment form of the page -->
<div class="appointment-form">
    <%= form_tag search_path, method: :get, id: "contactForm2", "data-toggle": 'validator' do %>
    <fieldset>
        <div class="form-group">
                <%= select_tag "search_specialite" ,options_from_collection_for_select(Speciality.all.map { |spec| spec }.sort_by { |spec| spec.name },:name, :name)  %>
        </div>
        <div class="form-group">
            <%= text_field_tag :search_lieu, params[:search_lieu], placeholder: "Où en avez-vous besoin ?", class: "form-control datepicker", id:"lieu"  %>
        </div>
        <div class="form-group">
            <input type="tel" id="tel2" class="form-control lg-round" placeholder="Your Phone" required data-error="NEW ERROR MESSAGE">
        </div>
        <div id="msgSubmit2" class="form-message hidden"></div>
        <%= submit_tag "Search",class: "btn-primary text-uppercase lg-round text-center", id: "form-submit2"%>	
    </fieldset>
    <% end %>
</div>
<!-- appointment form of the page end here -->